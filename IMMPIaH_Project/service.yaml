apiVersion: v1
kind: Service
metadata:
  name: patient-monitoring-service
spec:
  type: NodePort
  selector:
    app: patient-monitoring
  ports:
    - port: 80
      targetPort: 5000
      nodePort: 30007
